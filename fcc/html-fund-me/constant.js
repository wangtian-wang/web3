// 存放abi文件 合约部署的address
//   abi来自编译好的合约文件
// artifacts/contracts/fundme.sol/fundme.json
// 合约部署的address 假设部署在本地的话 是在控制台里面找到对应的合约的部署地址

// 将本地合约部署的地址添加到matamask的地址上面

// 将本地部署成功后的第一个账户的private key 导入 在metamask的 导入账户里面,将账户切换为该账户既当前合约部署网络对应的账户

// 不同的账户 只能在该账户对应的部署环境中使用

// 再将本地的合约部署中断后,重新启动的话 需要将metamask 对应的账户重新 refresh

// contract fund function 是将当前账户的 钱 转出  withdraw是将发送的交易取消
